import MemberThreadModule from '../../../modules/Chat/components/memberThreadListings'

const MemberThread = () => {
  return (
    // <div className="flex min-h-screen flex-col items-center justify-center py-2">
    <div className="min-h-screen w-full">
      <MemberThreadModule />
    </div>
  )
}

export default MemberThread